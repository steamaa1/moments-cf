# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea
__pycache__/
*.py[cod]

# Local env files
.env
.env.*
!.env.example

.pnpm-store

upload
*.sqlite
moments.exe

# Local planning and assessment artifacts
task_plan.md
findings.md
progress.md
AGENTS.md

# Cloudflare local development state
.wrangler/

# Node dependencies & package manifests (worker local run artifacts)
node_modules
worker/pnpm-lock.yaml

# Per-account Cloudflare bootstrap output (contains resource IDs, never commit)
worker/wrangler.local.toml
.moments-cf-bootstrap.json
worker/wrangler.build.toml

# Local-only dev runner (pure-Node harness, never commit)
worker/dev-server.mjs
